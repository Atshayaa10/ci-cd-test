import setuptools

setuptools.setup(
    name="calculator",
    version="1.0",
    packages=setuptools.find_packages(),
    install_requires=[],
    tests_require=["unittest"],
    test_suite="tests",
)
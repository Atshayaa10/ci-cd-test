import setuptools
import unittest

setuptools.setup(
    name='calculator',
    version='1.0',
    packages=['calculator'],
    install_requires=[],
    tests_require=['unittest'],
    test_suite='tests'
)